#include "get_path.h"

char *where(char *command, struct pathelement *p)
{
    // char cmd[128], *ch;
    // int found = 0;
    // struct pathelement *found, *tmp;
    // //p = get_path();
    // while (p->next != NULL)
    // { // WHICH
    //     //sprintf(cmd, "%s/gcc", p->element);
    //     if (access(cmd, X_OK) == 0)
    //     {
    //         //found = 1;
    //         printf("[%s]\n", cmd);
    //         tmp = calloc(1, sizeof(struct pathelement));
    //         found = tmp;
    //         p = p->next;
    //     }
    // }



}